pipeline {
    agent any

    options {
        timestamps()
        disableConcurrentBuilds()
        timeout(time: 30, unit: 'MINUTES')
    }

    stages {
        stage('Complete') {
            steps {
                echo 'Processing.....'
                echo 'Complete'
            }
        }
    }

    post {
        success {
            script {
                currentBuild.description = '<a href="http://www.163.com" target="_blank">Complete</a>'
            }
        }
    }
}
