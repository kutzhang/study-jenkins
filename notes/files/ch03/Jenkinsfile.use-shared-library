@Library("study") _
@Library("study") import static com.sanlea.study.jenkins.MessageManager.*

pipeline {
    agent {
        label "maven"
    }

    stages {
        stage('standard class') {
            steps {
                say this, "how do you turn this on?"
            }
        }

        stage('vars') {
            steps {
                sayHello this, 'show me the money!'
            }
        }
    }
}
