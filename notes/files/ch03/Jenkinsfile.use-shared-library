@Library("study")
import static com.sanlea.study.jenkins.MessageManager.say

pipeline {
    agent {
        label "maven"
    }

    stages {
        stage('standard class') {
            steps {
                script {
                    say this, "how do you turn this on?"
                }
            }
        }

        stage('vars') {
            steps {
                sayHello this, 'show me the money!'
            }
        }
    }
}
