@Library("study") _
import com.sanlea.study.jenkins.MessageManager

pipeline {
    agent {
        label "maven"
    }

    stages {
        stage('standard class') {
            steps {
                MessageManager.say steps, "how do you turn this on?"
            }
        }

        stage('vars') {
            steps {
                sayHello this, 'show me the money!'
            }
        }
    }
}
